# StoryFlow AI 项目介绍文档

> 仓库地址：https://github.com/xiaozhang-art/storyflow-ai  
> 文档生成日期：2026-07-02

---

## 一、项目概述

**StoryFlow AI** 是一个基于 **Multi-Agent Workflow** 的 AI 漫剧自动生成平台。用户输入一段创意文字，系统会自动完成以下全流程，最终输出 MP4 漫剧视频：

```
创意输入 → 剧本 → 角色设计 → 分镜 → 图片 → 图生视频 + 配音 → 视频合成 → story.mp4
```

核心特点：

- **7 个 AI Agent 协作**：Script / Character / Storyboard / Image / Image-to-Video / Voice / Video
- **StoryFlow Runtime V3**：事件总线、黑板、产物管理、Director 决策、质量门控
- **DSL 驱动工作流**：通过 `workflows/comic.yaml` 定义管线步骤与依赖
- **Montage 渲染引擎**：内置 FFmpeg 视频合成、TTS、字幕、转场等能力
- **Web 前端**：React + Ant Design，支持创建故事、实时进度、结果展示

---

## 二、仓库结构

当前工作区 `F:\storyai` 包含：

```
F:\storyai\
├── storyflow-ai/          # 主项目（前后端 + 部署配置）
│   ├── backend/           # Python FastAPI 后端
│   ├── frontend/          # React + Vite 前端
│   ├── deploy/            # Docker Compose、Nginx、环境变量模板
│   └── scripts/           # 数据库初始化等脚本
└── OpenMontage/           # 占位目录（Montage 引擎已内嵌于 backend/runtime/montage/）
```

### 2.1 后端目录（`storyflow-ai/backend/`）

| 目录/文件 | 说明 |
|-----------|------|
| `main.py` | FastAPI 应用入口，生命周期管理 |
| `configs/settings.py` | Pydantic 配置（LLM、数据库、API 供应商等） |
| `agents/` | 7 个 AI Agent 实现 |
| `api/` | REST + WebSocket 路由 |
| `models/` | SQLAlchemy ORM 模型 |
| `schemas/` | Pydantic 请求/响应模型 |
| `services/` | 业务逻辑层 |
| `repositories/` | 数据访问层 |
| `tasks/runner.py` | 后台任务执行器（进度追踪 + DB 持久化） |
| `workflows/` | DSL 工作流定义（comic.yaml / novel.yaml） |
| `runtime/` | StoryFlow Runtime 核心（Director、EventBus、Montage 等） |

### 2.2 前端目录（`storyflow-ai/frontend/`）

| 页面 | 路由 | 功能 |
|------|------|------|
| `HomePage` | `/` | 创建漫剧、历史列表 |
| `StoryPage` | `/story/:id` | WebSocket 实时生成进度 |
| `ResultPage` | `/story/:id/result` | 视频、剧本、角色、分镜展示 |

### 2.3 部署目录（`storyflow-ai/deploy/`）

| 文件 | 说明 |
|------|------|
| `docker-compose.yml` | PostgreSQL + Redis + Backend |
| `.env.example` | 环境变量模板 |
| `nginx/default.conf` | Nginx 反向代理 |
| `init.sql` | PostgreSQL 初始化 |

---

## 三、技术架构

### 3.1 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | React 18, TypeScript, Vite 5, Ant Design 5, React Router 6 |
| 后端 | Python 3.11+, FastAPI, SQLAlchemy 2.0 (async), Pydantic 2.0 |
| LLM | OpenAI 兼容 API（GPT-4o / Qwen / DeepSeek），LangChain |
| 图片 | DashScope 通义万相 / DALL-E / Mock |
| 图生视频 | Kling / Runway / Mock |
| 语音 | OpenAI TTS / DashScope / ElevenLabs / Mock |
| 视频合成 | FFmpeg + Montage VideoComposer |
| 数据库 | PostgreSQL（生产）/ SQLite（本地开发） |
| 缓存 | Redis（生产）/ 内存模式（本地开发） |

### 3.2 工作流管线（comic.yaml）

```
script → character → storyboard → image → image_to_video ─┐
                                                          ├→ video
                              storyboard → voice ─────────┘
```

- `image_to_video` 与 `voice` 可并行执行（由 DSL 定义依赖关系）
- 每步完成后可触发质量门控（QualityEngine）
- Director 可在失败时决策：重试 / 回退 / 跳过 / 重写 Prompt

### 3.3 Runtime 核心组件

| 组件 | 职责 |
|------|------|
| **WorkflowEngine** | 管线编排，执行 Agent 链 |
| **Director** | LLM 决策大脑（PROCEED / RETRY / ROLLBACK / SKIP 等） |
| **EventBus** | 发布/订阅，解耦步骤与进度通知 |
| **ArtifactManager** | 中间产物与检查点存储 |
| **SessionManager** | 会话管理，支持断点续传 |
| **QualityEngine** | 多维度质量检测 |
| **MontageAdapter** | 视频渲染桥接（TTS、字幕、转场、合成） |
| **StoryMemory** | 多维记忆（角色、场景、时间线等） |

---

## 四、API 接口

### 故事

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/story` | 创建故事 |
| GET | `/api/story` | 故事列表 |
| GET | `/api/story/{id}` | 故事详情 |
| POST | `/api/story/{id}/generate` | 启动生成 |
| GET | `/api/story/{id}/result` | 生成结果 |

### 任务

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/task/{id}` | 任务状态 |
| GET | `/api/task/by-story/{story_id}` | 按故事查任务 |
| WS | `/api/task/{id}/ws` | WebSocket 实时进度 |

### 系统

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |
| GET | `/api/runtime/stats` | Runtime 统计 |

---

## 五、配置说明

配置文件位于 `storyflow-ai/backend/.env`（可从 `deploy/.env.example` 复制）。

### 必填项

| 变量 | 说明 |
|------|------|
| `LLM_API_KEY` | LLM API Key（生成剧本等步骤必需） |
| `LLM_MODEL` | 模型名，默认 `gpt-4o` |
| `LLM_BASE_URL` | API 地址，默认 `https://api.openai.com/v1` |

### 本地开发推荐配置

| 变量 | 推荐值 | 说明 |
|------|--------|------|
| `DATABASE_URL` | `sqlite+aiosqlite:///./storyflow.db` | 无需 Docker PostgreSQL |
| `REDIS_URL` | `memory://local` | 内存模式，无需 Redis |
| `DEBUG` | `true` | 开启调试日志 |
| `IMAGE_API_PROVIDER` | `mock` | 无图片 API 时使用 Mock |
| `VOICE_API_PROVIDER` | `mock` | 无 TTS API 时使用 Mock |
| `I2V_API_PROVIDER` | `mock` | 无图生视频 API 时使用 Mock |

### 生产环境配置

| 变量 | 说明 |
|------|------|
| `DATABASE_URL` | `postgresql+asyncpg://storyflow:storyflow@localhost:5432/storyflow` |
| `REDIS_URL` | `redis://localhost:6379/0` |
| `IMAGE_API_PROVIDER` | `dashscope` / `openai` |
| `I2V_API_PROVIDER` | `kling` / `runway` |
| `VOICE_API_PROVIDER` | `dashscope_tts` |

---

## 六、如何运行项目

### 环境要求

- **Python 3.11+**（推荐 3.12；系统默认 3.8 不可用）
- **Node.js 18+**
- **FFmpeg**（视频合成步骤需要，需加入 PATH）
- **LLM API Key**（剧本生成必需）
- Docker（可选，用于 PostgreSQL + Redis 生产部署）

---

### 方式一：本地开发（推荐，无需 Docker）

#### 1. 后端

```powershell
cd F:\storyai\storyflow-ai\backend

# 创建虚拟环境（使用 Python 3.12）
py -3.12 -m venv venv
.\venv\Scripts\Activate.ps1

# 安装依赖
pip install -r requirements.txt

# 配置环境变量（已预配置 SQLite + 内存 Redis + Mock 模式）
# 编辑 .env，填入 LLM_API_KEY

# 启动后端
python -m uvicorn main:app --host 127.0.0.1 --port 8000 --reload
```

启动成功后访问：http://127.0.0.1:8000/health  
应返回：`{"status":"ok","runtime":true,...}`

#### 2. 前端

```powershell
cd F:\storyai\storyflow-ai\frontend

npm install
npm run dev
```

浏览器打开：**http://127.0.0.1:3000**

前端通过 Vite 代理将 `/api` 和 `/storage` 转发到后端 8000 端口。

#### 3. 使用流程

1. 打开 http://127.0.0.1:3000
2. 填写漫剧标题、题材、创意描述
3. 点击「生成漫剧」
4. 在进度页查看 WebSocket 实时状态
5. 完成后跳转结果页查看视频与剧本

> **注意**：Mock 模式下图片/配音/视频为占位数据，完整成片需配置真实 API Key 并安装 FFmpeg。

---

### 方式二：Docker Compose 部署

```powershell
# 先启动 Docker Desktop
cd F:\storyai\storyflow-ai\deploy
copy .env.example .env
# 编辑 .env：改用 PostgreSQL + Redis，填入 LLM_API_KEY

docker compose up -d
```

服务：

| 服务 | 端口 | 说明 |
|------|------|------|
| postgres | 5432 | PostgreSQL 16 |
| redis | 6379 | Redis 7 |
| backend | 8000 | FastAPI 后端 |

仅启动基础设施（本地跑后端时）：

```powershell
docker compose up -d postgres redis
```

---

## 七、已修复的问题

为使项目可在本地正常运行，本次修复了以下问题：

| 问题 | 修复 |
|------|------|
| `.env` 含未定义字段导致启动崩溃 | `settings.py` 增加 `extra="ignore"` |
| SQLite 不支持 PostgreSQL JSONB | `character.py` 改用通用 `JSON` 类型 |
| SQLite 引擎参数不兼容 | `database.py` 按数据库类型调整连接池 |
| 无 Docker 时 Redis/PostgreSQL 不可用 | 支持 `sqlite+aiosqlite` 与 `memory://local` |
| `WorkflowEngine` 缺少 `load_dsl` | 实现 YAML DSL 加载 |
| 故事列表 API 返回格式错误 | `list_stories` 返回 `{items: [...]}` |
| 前端 TypeScript 编译错误 | Ant Design `colorPrimary`、移除未使用变量 |
| Vite WebSocket 代理不正确 | `/api` 代理启用 `ws: true` |

---

## 八、测试

```powershell
cd F:\storyai\storyflow-ai\backend
.\venv\Scripts\Activate.ps1
pip install pytest
python tests/test_v15_integration.py
```

当前 23 项 Runtime 集成测试全部通过。

---

## 九、容错与降级

| 模块 | 降级策略 |
|------|----------|
| TTS | OpenAI → DashScope → ElevenLabs → Google → Piper → 静默占位 |
| 图片 | DashScope → DALL-E → Mock |
| 图生视频 | Kling → Runway → FFmpeg 静态图转视频 |
| 视频合成 | Montage 引擎 → Legacy FFmpeg concat |
| Agent 失败 | tenacity 3 次重试 → Director 决策（SKIP / ROLLBACK） |

---

## 十、常见问题

**Q: 启动报 `ValidationError: Extra inputs are not permitted`？**  
A: 确保使用最新 `settings.py`（含 `extra="ignore"`），或删除 `.env` 中过时字段。

**Q: 数据库连接失败？**  
A: 本地开发使用 `DATABASE_URL=sqlite+aiosqlite:///./storyflow.db`，无需 PostgreSQL。

**Q: Redis 连接失败？**  
A: 设置 `REDIS_URL=memory://local` 使用内存模式。

**Q: 生成卡在剧本步骤？**  
A: 检查 `LLM_API_KEY` 是否有效，`LLM_BASE_URL` 是否可访问。

**Q: 视频合成失败？**  
A: 确认系统已安装 FFmpeg 并加入 PATH：`ffmpeg -version`

**Q: Docker 启动失败？**  
A: 确认 Docker Desktop 已运行；Windows 需启用 WSL2/Hyper-V。

---

## 十一、License

MIT
